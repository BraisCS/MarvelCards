/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["i.annihil.us"],
  },
  staticPageGenerationTimeout: 900, // Incrementa el tiempo de espera a 900 segundos
};

module.exports;
